# openfast_basic

This is a standard OpenFAST run configuration using the full-scale semisub model. The displacement/velocity outputs from this run are the same used in `openfast_hybrid_validation`. It uses the `validation_test.lsx` Simulink model.